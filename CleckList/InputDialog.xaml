<Window x:Class="CleckList.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input URL" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Enter URL:" Margin="10" />
        <TextBox x:Name="textBox" Margin="10" Grid.Row="1" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="10">
            <Button Content="OK" Width="75" Margin="5" Click="OK_Click" />
            <Button Content="Cancel" Width="75" Margin="5" Click="Cancel_Click" />
            <Button Content="Remove" Width="75" Margin="5" Click="Remove_Click" />
        </StackPanel>
    </Grid>
</Window>